<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android" xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:id="@+id/menu_toolbar_search"
          android:title="@string/menu_main_search"
          android:orderInCategory="100"
          android:icon="@drawable/ic_search"
          app:showAsAction="ifRoom"
    />

    <item android:id="@+id/menu_toolbar_settings"
          android:orderInCategory="100"
          android:icon="@drawable/ic_menu"
          app:showAsAction="always">
        <menu
            android:id="@+id/menu_main_settings">
            <item android:id="@+id/menu_settings_currency"
                  android:title="@string/menu_main_currency"/>

        </menu>

    </item>

</menu>